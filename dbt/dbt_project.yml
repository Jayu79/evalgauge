name: evalgauge
version: 1.0.0
config-version: 2

profile: evalgauge
model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

models:
  evalgauge:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: view
      +schema: marts
    metrics:
      +materialized: view
      +schema: metrics

